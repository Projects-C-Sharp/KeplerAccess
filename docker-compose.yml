# ─────────────────────────────────────────
# API — ASP.NET Core 10
# ─────────────────────────────────────────
version: '3.9'

services:
  admin-tickets:
    container_name: admin-tickets

    build:
      context: .
      dockerfile: Dockerfile

    image: access-tickets:latest

    ports:
      - "5207:8080"

    restart: unless-stopped


